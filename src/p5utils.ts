import * as p5 from "p5";

const sketchToPixels = (
  pixelNum: number,
  gridHeight: number,
  gridWidth: number
) => {
  for (const pixel = 0; pixel < pixelNum; pixel++) {
    p5.prototype.get();

    // const blockPixelColor = p5.get(wblck, hblock);
  }
};
